<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bancoprueba">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BancoPrueba">
        <activity
            android:name=".historial.HistorialActivity"
            android:exported="true" />
        <activity
            android:name=".historial.HistorialTransaccionesActivity"
            android:exported="true" />
        <activity android:name=".views.VoucherActivity" />
        <activity android:name=".views.DepositoActivity" />
        <activity android:name=".views.ConsultarSaldoActivity" />
        <activity android:name=".views.RetiroActivity" />
        <activity android:name=".views.RegisterNewBankActivity" />
        <activity android:name=".views.MenuActivity" />
        <activity android:name=".views.RegistroActivity" />
        <activity
            android:name=".views.LoginActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>